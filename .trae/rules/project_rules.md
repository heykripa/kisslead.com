
1. System Prompt (Front-end & UX)
"Act as a Senior Front-end Engineer and UX Designer. Your task is to build a high-fidelity Next.js landing page based on the attached screenshot. Use Tailwind CSS for layout, shadcn/ui for core components (Accordions, Buttons, Cards, Forms), and a combination of Framer Motion (for entry transitions and micro-interactions) and GSAP ScrollTrigger (for complex scroll-linked effects and the marquee/carousel sections). Ensure the typography matches the bold, minimalist aesthetic, using a Sans-Serif font like Inter or Geist. Implement a mobile-first responsive design.

2. Animation Strategy
GSAP: Use for the "Hero Slider" (horizontal scroll or staggered image appearance) and the "Services" horizontal carousel.
Framer Motion: Use for text reveal animations (split-text effect on headings), button hover states, and the FAQ accordion opening transitions.